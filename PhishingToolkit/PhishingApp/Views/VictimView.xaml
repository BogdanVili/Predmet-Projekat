<UserControl x:Class="PhishingApp.Views.VictimView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:PhishingApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">
    <Grid>
        <Grid.Background >
            <SolidColorBrush Color="LightGray"/>
        </Grid.Background>

        <Border Background="LightSlateGray" Margin="30" HorizontalAlignment="Left" VerticalAlignment="Top" Height="125" Width="300"  Padding="20">

            <lvc:CartesianChart LegendLocation="None">

                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Count" MinValue="0"/>
                </lvc:CartesianChart.AxisX>

                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Labels="{Binding PieChartModel.Labels}" />
                </lvc:CartesianChart.AxisY>

                <lvc:CartesianChart.Series>
                    <lvc:RowSeries
            Values="{Binding PieChartModel.Values}"
            DataLabels="True"
            LabelPoint="{Binding PieChartModel.PointLabel}" />
                </lvc:CartesianChart.Series>

            </lvc:CartesianChart>
        </Border>


        <DataGrid Name="VictimsDataGrid" VerticalAlignment="Top" IsReadOnly="True" HorizontalAlignment="Center" Background="LightGray" RowBackground="LightYellow" AlternatingRowBackground="LightBlue"
		  HorizontalScrollBarVisibility="Visible"  
          VerticalScrollBarVisibility="Visible" Height="576" Width="1040" Margin="0,190,0,0" 
		  GridLinesVisibility="Vertical" HeadersVisibility="All" ItemsSource="{Binding StatisticsModel.Victims}" >

        </DataGrid>
    </Grid>
</UserControl>
